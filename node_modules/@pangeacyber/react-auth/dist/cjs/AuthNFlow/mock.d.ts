import { FC, ReactNode } from "react";
import { APIResponse } from "@src/types";
import { FlowState, FlowStep } from "./types";
export interface MockFlowContextType {
    step?: FlowStep;
    error: APIResponse | undefined;
    loading: boolean;
    flowData: FlowState;
    callNext: (endpoint: FlowStep) => void;
    reset: () => void;
}
export interface MockFlowProviderProps {
    children: ReactNode;
}
export declare const MockFlowContext: import("react").Context<MockFlowContextType>;
export declare const MockFlowProvider: FC<MockFlowProviderProps>;
export declare const useMockFlow: () => MockFlowContextType;
